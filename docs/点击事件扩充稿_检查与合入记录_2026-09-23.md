# 2026-09-23点击事件扩充稿：检查与合入记录

依据：docs/叙事编辑交付/私人助手_所有点击事件缺口填补与事件扩充稿_2026-09-23.md

## 检查结论

27个缺口ID全部匹配当前代码。第三部分标称26个启用事件，其中17个匹配，9个已停用；另外9个当前事件没有扩写稿。没有为凑数量重新启用旧热点。

主要调整：游戏盘不写成音乐盘；猫统一为黑白猫；不增加不存在的房间编号、刻痕密码、纸面隐藏字、通电声或强制归还八音盒要求；未具备关灯状态的房间不使用游戏房dark变量。保留原稿的冷面自嘲与克制依恋方向。

## 实际合入

- 27个原有退出/跳转选项加入首次、重复与指定情境回应，独立于普通闲聊覆盖规则。
- 4处关键操作：值班守则、挂回听筒、退出档案柜、打开随身八音盒。
- 17个启用事件调整进入、复访或情境文本；其中9个交流事件增加可见追问和各自收尾。
- 14个环境观察继续保持无选择按钮；其中相关扩写通过复访和情境观察呈现。
- 回应次数存入当前叙事存档；兼容已有选择记录；重新探索与录像轮回仍清本轮记录。
- 档案退出由近景组件传递已读/未归还状态；已归还不误提示。
- 打开八音盒的回应有20秒间隔，合上、音量与进度操作不刷对白。
- 修复继续游戏调用台词后立即隐藏的问题。
- 登记复访读取访客/留白旗标，不再对已经登记的玩家说还没想好。

## 原稿覆盖边界

已合入扩写：visitor-register、stopped-clock、coat-owner、missing-room、wet-step、cat-seat、disc-margin、tool-care、unrecorded-answer、far-away、spliced-label、chair-distance、door-name、no-reflection、borrowed-light、sparse-signal、loose-contact。

当前停用、未重接入口：box-outline、sofa-place、prototype-mistake、empty-file、pencil-note、room-index、fifth-seat、empty-audience、forgotten-line。

原稿第三部分遗漏、保留现有事件文本：antenna-hum、rain-cup、signal-reports、missing-screw、cold-plug、unnamed-box、blank-index、covered-chair、stored-clock。其中rain-cup、signal-reports、unnamed-box的指定后置回应已按第一部分接入。

## 逐项审定

|定位|字段|处理原因|
|---|---|---|
|CHOICE.coat-owner.keep.continue|条件差分|所在房间没有dark状态；适用夜雨描述转为复次候选，不读取游戏房的关灯状态。|
|CHOICE.missing-room.map.continue|条件差分|所在房间没有dark状态；适用夜雨描述转为复次候选，不读取游戏房的关灯状态。|
|CHOICE.sparse-signal.allowed.stay|条件差分|所在房间没有dark状态；适用夜雨描述转为复次候选，不读取游戏房的关灯状态。|
|CHOICE.rain-cup.unsure.continue|条件差分|所在房间没有dark状态；适用夜雨描述转为复次候选，不读取游戏房的关灯状态。|
|CHOICE.loose-contact.pause.continue|条件差分|所在房间没有dark状态；适用夜雨描述转为复次候选，不读取游戏房的关灯状态。|
|CHOICE.door-name.return.continue|选项|改为准备返回，不暗示点击后已经传送；仍由房间里的门行走。|
|CHOICE.borrowed-light.repair.continue|首次/重复|原选项是微热，不能直接断言玩家被烫伤。|
|CHOICE.sparse-signal.allowed.script|旗标回应|admitted_unknown记录的是允许助手承认不知道，不是玩家自认不知道。|
|SCENE.duty.bulletin|高紧张|去掉画面与守则正文均不存在的铅笔涂黑条款。|
|CONTROL.archive-exit|默认回应|退出不等于推平抽屉；由近景组件提供已读未归还事实再给提醒。|
|missing-room|场景扩写|修正静态画面、物件类型或不存在的线索/行为；采用审定后的描写。|
|wet-step|场景扩写|修正静态画面、物件类型或不存在的线索/行为；采用审定后的描写。|
|cat-seat|场景扩写|修正静态画面、物件类型或不存在的线索/行为；采用审定后的描写。|
|disc-margin|场景扩写|修正静态画面、物件类型或不存在的线索/行为；采用审定后的描写。|
|unrecorded-answer|场景扩写|修正静态画面、物件类型或不存在的线索/行为；采用审定后的描写。|
|far-away|场景扩写|修正静态画面、物件类型或不存在的线索/行为；采用审定后的描写。|
|spliced-label|场景扩写|修正静态画面、物件类型或不存在的线索/行为；采用审定后的描写。|
|chair-distance|场景扩写|修正静态画面、物件类型或不存在的线索/行为；采用审定后的描写。|
|door-name|场景扩写|修正静态画面、物件类型或不存在的线索/行为；采用审定后的描写。|
|no-reflection|场景扩写|修正静态画面、物件类型或不存在的线索/行为；采用审定后的描写。|
|borrowed-light|场景扩写|修正静态画面、物件类型或不存在的线索/行为；采用审定后的描写。|
|sparse-signal|黑暗差分|天台雨夜作为复访描写，不增加不存在的关灯条件。|
|sparse-signal|场景扩写|修正静态画面、物件类型或不存在的线索/行为；采用审定后的描写。|
|loose-contact|场景扩写|修正静态画面、物件类型或不存在的线索/行为；采用审定后的描写。|
|visitor-register.revisit|称呼记忆|已登记后复访不再说还没想好，留白选择也保留其意愿。|

## 校验

- TypeScript与生产构建通过。
- 现有31项叙事、场景、动作与反馈检查通过；新增5项针对回应、旧存档、旗标、退出事实和追问可达性的检查通过。
- 浏览器实测：登记首次/重复回应；值班守则打开后气泡可见；工具箱记忆分支进入作品面板后回应可见；新增追问；档案已读未归还退出提醒；八音盒打开回应。
- 27项首次/重复和对应条件由自动检查逐条验证，并非27条都做了手工浏览器通关。

## 备份与实现

修改前源码备份：S:/work/实习相关/website-backups/editorial-gap-20260923-002437/avg。

叙事数据：src/avg/narrative/events.json；操作回应：src/avg/narrative/interaction-responses.json。首次/重复选择与条件解析由narrative/types.ts统一处理，不在组件里硬编码各句文本。

审定映射脚本：scripts/merge-gap-editorial-20260923.mjs。当前稿件保持原样。
